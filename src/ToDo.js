function ToDo() {
  function addItem() {

  }

  function deleteItem() {
    
  }

  return (
    <div>
      <button onClick={deleteItem}>Delete</button>
      <button onClick={addItem}>Add</button>
    </div>
  );
}

export default ToDo;
